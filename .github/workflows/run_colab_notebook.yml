name: Test Schedule Workflow

on:
  push:
    branches:
      - main
  schedule:
    - cron: '*/1 * * * *' # 1분마다 실행

jobs:
  test-schedule:
    runs-on: ubuntu-latest

    steps:
      - name: Print current time
        run: 'echo "Current time: $(date)"'
