name: Test Schedule Workflow

on:
  schedule:
    - cron: '*/5 * * * *' # 1분마다 실행

jobs:
  test-schedule:
    runs-on: ubuntu-latest
    
    steps:
      - name: Print current time
        run: echo "Current time: $(date)"
